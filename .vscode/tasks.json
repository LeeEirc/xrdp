{
    "version": "2.0.0",
    "tasks": [
        {
            "type": "shell",
            "label": "build xrdp",
            "command":"./bootstrap && ./configure --enable-neutrinordp --enable-devel-all --enable-devel-logging",
        },
        {
            "type": "shell",
            "label": "make xrdp",
            "command":"make && make install && ldconfig",
        },
        
    ]
}