{
    "version": "2.0.0",
    "tasks": [
        {
            "type": "shell",
            "label": "build xrdp",
            "command":"./bootstrap && ./configure --enable-neutrinordp --enable-devel-all",
        },
        {
            "type": "shell",
            "label": "make xrdp",
            "command":"make && make install && ldconfig",
        },
        
    ]
}